eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
spring:
  application:
    name: user-service
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/soulkanarprograms/config-server-repository
  profiles:
      active: dev
